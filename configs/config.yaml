PORT: 5000                  
ENV: development            # development, staging, production

DB_HOST: localhost
DB_PORT: 5432     
DB_USER: postgres 
DB_PASSWORD: eaglevasasql       
DB_NAME: vkr_wms            
DB_SSLMODE: disable         

DB_MAX_OPEN_CONNS: 25    
DB_MAX_IDLE_CONNS: 5     
DB_CONN_MAX_LIFETIME: 30m

DB_USE_MIGRATE: true
DB_FULL_LOGS: false


JWT_SECRET: X9v2#kLp8!zR4dQ1@bT7mS0wY5eH6uF3
JWT_ACCESS_EXPIRATION: 720h
JWT_REFRESH_EXPIRATION: 720h

ITEMS_PAGE_SIZE: 30

LOG_LEVEL: debug            # debug, info, warn, error (для zap/logrus)

ALLOWED_ORIGINS:
  - http://localhost:3000    
  - http://127.0.0.1:3000

SERVER_READ_TIMEOUT: 15
SERVER_WRITE_TIMEOUT: 15
SERVER_IDLE_TIMEOUT: 60